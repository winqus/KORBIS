import React, { useRef, useState } from "react";
import { CameraOld } from "@/components/CameraOld";
import { ExpoItemCamera } from "@/components/ExpoItemCamera";

const Camera = () => {
  return <ExpoItemCamera />;
};

export default Camera;
